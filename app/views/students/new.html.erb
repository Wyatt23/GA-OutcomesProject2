<body class="student-body">

<div class="form">
  <%= form_for @student, html: {class:"enroll_form"} do |f| %>

    <div class="formfield">
      <%= f.label :first_name, :class=>"label" %>:
      <%= f.text_field :first_name, :class=>"input" %>
    </div>

    <div class="formfield">
      <%= f.label :last_name, :class=>"label" %>:
      <%= f.text_field :last_name, :class=>"input" %>
    </div>

    <div class="formfield">
      <%= f.label :ga_focus, :class=>"label" %>:
      <%= f.text_field :ga_focus, :class=>"input" %>
    </div>

    <div class="formfield">
      <%= f.label :cohort_number, :class=>"label" %>:
      <%= f.text_field :cohort_number, :class=>"input" %>
    </div>

    <%= f.submit "Enroll New Student", :class=>"submit_button buttontxt"%>
    <%end%>
  </div>

</body>
