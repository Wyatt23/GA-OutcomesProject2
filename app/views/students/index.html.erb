  <div class="enroll_button"><%=link_to"Enroll New Student!",new_student_path%> </div>

<% @students.group_by do |student| %>
  <p class="student_name"><%=link_to student.last_name,student_path(student)%>, <%=student.first_name%>, <%=student.ga_focus%><%=student.cohort_number%></p>
<% end %>
